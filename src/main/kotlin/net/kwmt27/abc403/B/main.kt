package net.kwmt27.abc403.B

fun main() {
    val T = readln().map { it }
    val U = readln().map { it }

    // T = tak??a?h?
    // U = nashi
    for (t in T) {
        if (t == '?') {

        } else {

        }
     }

    for (u in U ){

    }


    println(T)
}